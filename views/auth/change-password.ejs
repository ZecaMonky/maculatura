<div class="row justify-content-center">
    <div class="col-md-5 col-lg-4">
        <div class="card p-4 shadow-sm border-0 mobbin-auth-card">
            <div class="text-center mb-4">
                <div class="mbbin-logo mb-4" style="font-size:2.5rem; font-weight:900; color:#333;"></div>
                <div class="mb-2" style="font-size:2rem; font-weight:700; letter-spacing:0.01em; color:#222; font-family:'Cakra',sans-serif;">Смена пароля</div>
                <% if (isFirstLogin) { %>
                    <div class="alert alert-warning">
                        Для продолжения работы необходимо сменить стандартный пароль на новый.
                    </div>
                <% } %>
            </div>
            <% if (typeof error !== 'undefined') { %>
                <div class="alert alert-danger">
                    <%= error %>
                </div>
            <% } %>
            <form action="/auth/change-password" method="POST" autocomplete="off">
                <div class="mb-3">
                    <input type="password" class="form-control mobbin-input" id="newPassword" name="newPassword" required autofocus placeholder="Новый пароль">
                    <div class="form-text">
                        Пароль должен содержать не менее 8 символов, включать заглавные и строчные буквы, цифры и специальные символы.
                    </div>
                </div>
                <div class="mb-3">
                    <input type="password" class="form-control mobbin-input" id="confirmPassword" name="confirmPassword" required placeholder="Подтвердите новый пароль">
                </div>
                <div class="d-grid mb-2">
                    <button type="submit" class="btn btn-dark mobbin-main-btn">Сменить пароль</button>
                </div>
            </form>
        </div>
    </div>
</div>